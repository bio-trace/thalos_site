# Place at /etc/thalos/env on the server (chmod 600, owner www-data)
PORT=3000
HOSTNAME=127.0.0.1
NODE_ENV=production

# Resend transactional email — required for /api/partner-gym
RESEND_API_KEY=
PARTNER_GYM_INBOX=hello@thalos.at
